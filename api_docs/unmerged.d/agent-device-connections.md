* New Grow Team device APIs register an anonymous pairing, exchange approved
  pairings, rotate runner credentials, and register runner workspaces and catalogs.
  Pairing credentials stay in strict JSON envelopes. Runner registration uses a
  separate bearer credential and never accepts local workspace paths.
