* [`POST /messages`](/api/send-message): The Grow Team pilot adds optional
  `agent_send_key` and `agent_send_metadata` local extensions. They bind one
  captured send intent to its original message ID. Conflicting retries fail
  without another send. Deleted messages retain their retry identity.
  Legacy clients can omit both fields. These extensions do not change the
  upstream feature level.
